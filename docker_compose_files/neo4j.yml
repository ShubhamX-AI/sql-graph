services:
  neo4j:
    image: neo4j:5
    container_name: neo4j_db
    restart: always

    environment:
      NEO4J_AUTH: neo4j/password

    ports:
      - "7474:7474"   # Browser UI
      - "7687:7687"   # Bolt protocol (for apps)

    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data: